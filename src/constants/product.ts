import { PRODUCT_CONTENT_TYPE } from "@/types";

export const DEFAULT_LABELS: Record<PRODUCT_CONTENT_TYPE, string> = {
  DESCRIPTION: "tabs.productDescription",
  SHIPPING: "tabs.productShipping",
  GENERAL: "tabs.productGeneral",
};
